import { mount, shallowMount } from '@vue/test-utils'
import Home from '../Page/Home.vue'

describe('Testar a pagina home', () => {
  it('Adicionar novo postagem', () => {

    const wrapper = shallowMount(Home, {

    })

  })
})